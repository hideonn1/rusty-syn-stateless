[package]
name = "lab_seguridad"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio   = { version = "1", features = ["full"] }
socket2 = { version = "0.5", features = ["all"] }
rand    = "0.8"
libc    = "0.2"
clap    = { version = "=4.0.32", features = ["derive"] }
